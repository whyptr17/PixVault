import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bgPrimary    = Color(0xFF0D0D0F);
  static const bgSecondary  = Color(0xFF141417);
  static const bgTertiary   = Color(0xFF1A1A1A); // From Stitch research
  static const bgOverlay    = Color(0x990D0D0F);

  // Accents
  static const accentPrimary   = Color(0xFF9B8FFF); // Violet
  static const accentSecondary = Color(0xFFC084FC); // Lavender
  static const accentGlow      = Color(0x269B8FFF);

  // Text
  static const textPrimary   = Color(0xFFFFFFFF); // From Stitch
  static const textSecondary = Color(0xFFBDBDBD); // From Stitch
  static const textTertiary  = Color(0xFF4A4A58);
  static const textInverse   = Color(0xFF0D0D0F);

  // Semantic
  static const success  = Color(0xFF4ADE80);
  static const warning  = Color(0xFFFACC15);
  static const danger   = Color(0xFFF87171);
  static const info     = Color(0xFF60A5FA);

  // Semantic backgrounds
  static const successBg = Color(0x1A4ADE80);
  static const warningBg = Color(0x1AFACC15);
  static const dangerBg  = Color(0x1AF87171);

  // Dividers
  static const divider       = Color(0x0FFFFFFF);
  static const dividerStrong = Color(0x1FFFFFFF);

  // Nav
  static const navBg     = Color(0xFF0F0F12);
  static const navBorder = Color(0x0FFFFFFF);
}
